import Link from"next/link";import{notFound}from"next/navigation";import Nav from"@/components/Nav";type S={id:string;name:string;city:string;country:string;address:string;types:string[];description:string;lat:number;lng:number};export default async function Shop({params}:{params:Promise<{id:string}>}){const{id}=await params;const res=await fetch(`${process.env.NEXT_PUBLIC_URL||"http://localhost:3000"}/api/shops`,{cache:"no-store"});const all:S[]=await res.json();const s=all.find(x=>x.id===id);if(!s)return notFound();return <main className="min-h-screen"><Nav/><article className="mx-auto max-w-4xl px-5 py-10"><Link href="/explore" className="font-bold text-violet">← Explore shops</Link><p className="mt-8 font-bold text-violet">{s.city}, {s.country}</p><h1 className="mt-2 text-4xl font-black">{s.name}</h1><p className="mt-5 text-lg leading-8 text-slate-700">{s.description}</p><section className="mt-8 rounded-2xl border bg-white p-6"><h2 className="text-lg font-black">Details</h2><p className="mt-4"><b>Address / area:</b> {s.address||"Not provided"}</p><div className="mt-4 flex flex-wrap gap-2">{s.types.map(t=><span key={t} className="rounded bg-lilac px-2 py-1 text-sm text-violet">{t.replace("_"," ")}</span>)}</div>{s.lat!==0&&<a target="_blank" rel="noreferrer" href={`https://www.openstreetmap.org/?mlat=${s.lat}&mlon=${s.lng}#map=16/${s.lat}/${s.lng}`} className="mt-6 inline-block rounded-lg bg-ink px-4 py-2 font-bold text-white">Open directions</a>}</section></article></main>}