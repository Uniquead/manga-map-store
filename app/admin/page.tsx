"use client";import{useEffect,useState}from"react";import Nav from"@/components/Nav";type S={id:string;name:string;city:string;country:string;status:string;description:string};export default function Admin(){const[s,setS]=useState<S[]>([]);const load=()=>fetch("/api/shops?all=true").then(x=>x.json()).then(setS);useEffect(load,[]);async function act(id:string,status:string){await fetch("/api/shops",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,status})});load()}return <main className="min-h-screen"><Nav/><section className="mx-auto max-w-5xl px-5 py-10"><h1 className="text-3xl font-black">Demo admin dashboard</h1><p className="mt-2 text-slate-600">No login in this demo. In a real app, protect this page with authentication.</p><div className="mt-6 overflow-hidden rounded-2xl border bg-white"><table className="w-full text-sm"><thead className="bg-lilac text-left"><tr><th className="p-3">Shop</th><th className="p-3">Location</th><th className="p-3">Status</th><th className="p-3">Moderate</th></tr></thead><tbody>{s.map(x=><tr className="border-t" key={x.id}><td className="p-3 font-bold">{x.name}<span className="block font-normal text-slate-500">{x.description}</span></td><td className="p-3">{x.city}, {x.country}</td><td className="p-3"><span className="rounded bg-lilac px-2 py-1 text-violet">{x.status}</span></td><td className="p-3"><div className="flex gap-2"><button onClick={()=>act(x.id,"published")} className="rounded bg-green-600 px-2 py-1 font-bold text-white">Approve</button><button onClick={()=>act(x.id,"pending")} className="rounded bg-amber-500 px-2 py-1 font-bold text-white">Pending</button><button onClick={()=>act(x.id,"rejected")} className="rounded bg-red-600 px-2 py-1 font-bold text-white">Reject</button></div></td></tr>)}</tbody></table></div></section></main>}